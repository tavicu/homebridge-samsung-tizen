// This is the name of the platform that users will use to register the plugin in the Homebridge config.json
export const PLATFORM_NAME = 'SamsungTizen';

// This must match the name of your plugin as defined the package.json
export const PLUGIN_NAME = 'homebridge-samsung-tizen';

// Heartbeat timeout, 8 seconds (6 ping + 2 for safety)
export const HEARTBEAT_TIMEOUT = 8 * 1000;

export const COOLDOWN_TIMEOUT = 1000 * 5;
